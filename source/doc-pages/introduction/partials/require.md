Call `require` in the appropriate place(s) for each adapter you want to use in your project

```ruby 
require 'rom-sql'      # use this if you installed sql adapter
require 'rom-http'     # and this for http
require 'rom-couchdb'  # ... etc 

```